 (* COMMENT *)
sample_normal:Double (mean:Double, stddev:Double where stddev != 0);
abs:Int where abs > 0 (val:Int);
pi:Int = 3;
max:Int (a:Int, b:Int) {
	if a > b {
		return a;
	} else {
		return b;
	}
	while true == true {
	
	}
}